apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  # here is the argocd app name for the cluster
  name: cluster-c1-demo
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: default
    # here is the url of the mgmt capi-cluster
    server: 'https://kubernetes.default.svc'
  source:
    # replace with private repo , branch and path
    repoURL: https://github.com/pli01/k8s-helmfiles.git
    targetRevision: argocd
    path: .
    plugin:
      name: helmfile-v1.0
      env:
        # here is the cluster name to create
        - name: ENV_NAME
          value: "c1-demo"
        - name: HELMFILE_FILE
          value: "helmfile.d/cluster-bootstrap"
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
#    automated:
#      prune: true
#      selfHeal: true
#      allowEmpty: true
#    retry:
#      limit: 5
#      backoff:
#        duration: 5s
#        maxDuration: 3m0s
#        factor: 2
