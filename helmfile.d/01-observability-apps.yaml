---
bases:
  - bases/helmDefaults.yaml
  - bases/environments.yaml

helmfiles:
- path: releases/01-observability/helmfile.yaml
  values:
  - {{ toYaml .Values | nindent 4 }}

missingFileHandler: Warn
